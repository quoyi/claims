# frozen_string_literal: true

source 'https://rubygems.org'
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

# Specify your gem's dependencies in power.gemspec.
gemspec

group :development do
  gem 'pg'
end

# To use a debugger
group :development, :test do
  gem 'byebug'
end
